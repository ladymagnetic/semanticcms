<?php
/* namespace */
namespace SemanticCms\Model;

/* Include(s) */
require_once 'ArticleType.enum.php';

/**
* Provides an enumeration  for the user permissions
*/
abstract class Article
{
	// private $username = "";
	// private $permissions = array();
	
	/* ---- Constructor / Destructor ---- */
	// /**
	// * constructor
	// * @params string $username the user's username
	// * @params array of type Permission $permissions the users permissions in cms
	// */
 	// public function __construct($username, array $permissions)
 	// {
		// //if(!is_string($username) || !is_array($permissions))
		// $this->username = $username;
		// $this->permissions = $permissions;
	// }
	
	/* ---- Methods ---- */
}
?>